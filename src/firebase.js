// Minimal firebase stub to satisfy imports during build.
// Exports null placeholders so existing imports don't fail.

export const auth = null;
export const db = null;

export default { auth, db };
